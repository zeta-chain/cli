# Smithery configuration file
startCommand:
  type: stdio

configSchema:
  type: object
  properties:
    debug:
      type: boolean
      description: Enable debug logging
      default: false

commandFunction: |-
  (config) => ({
    command: 'npx',
    args: ['-y', 'zetachain@7.0.1-rc2', 'zetachain-mcp'],
    env: config.debug ? { DEBUG: 'true' } : {}
  })
